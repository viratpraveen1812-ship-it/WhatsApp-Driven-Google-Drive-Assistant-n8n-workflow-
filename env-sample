# General n8n configuration
# You can keep these as-is for a local Docker setup
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
N8N_USER=your_username
N8N_PASSWORD=your_password

# Twilio Credentials
# Replace with your Twilio Account SID and Auth Token
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here

# Google Credentials (for OAuth2)
# These are used for Google Drive and Google Docs
# Replace with the client ID and secret from your Google Cloud Project
GOOGLE_OAUTH_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret_here
# Note: You must enable the Google Drive API and Google Docs API for your project.
# Also, you need to add the n8n user's email address as a "Test User" in the OAuth consent screen.
# The OAuth callback URL for n8n is typically http://<your-n8n-url>:<port>/rest/oauth2-credential/callback

# Google Gemini Credentials (for AI Summarization)
# Replace with your Gemini API Key
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# Other Optional Settings
NODE_ENV=production

```
